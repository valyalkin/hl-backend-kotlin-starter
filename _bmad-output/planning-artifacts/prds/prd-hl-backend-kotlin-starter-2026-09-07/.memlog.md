---
topic: PRD for the Spring Boot + Kotlin starter service (internal baseline template for the backend stack)
updated: 2026-09-09T14:23
---

- (decision) Confirmed constraints: build tool = Gradle (Kotlin DSL per brief); CI = GitHub Actions; audience = internal only for now (no public/open-source distribution in v1). Stakes = internal/foundational rigor.
- (decision) Working mode = Fast path: batch gaps into consolidated questions, then draft full PRD with [ASSUMPTION] tags, iterate with user.
- (decision) A: Version strategy = latest stable at architecture time for Kotlin, Spring Boot, JVM LTS, Gradle. No hard pins in PRD; architecture step sets exact numbers. Detail -> addendum.
- (decision) B: Example slice = 'widgets' resource, REST CRUD -> Postgres (JPA + Flyway) -> Redis cache-aside on read-by-id; unit + Testcontainers integration tests over full path.
- (decision) C: Layers domain/application/adapter-in/adapter-out; boundary rules enforced by an automated build-failing test (ArchUnit or Konsist, tool chosen in architecture).
- (decision) D: Observability = Actuator liveness/readiness groups + /actuator/health + /actuator/prometheus; Micrometer->Prometheus; OTLP trace exporter (endpoint from env, no-op when unset); structured JSON logs to stdout.
- (decision) E: Image built via Spring Boot Gradle plugin bootBuildImage (buildpacks, no Dockerfile). Publish on push to main only. Image tag = git short SHA + latest on main. Non-root, graceful shutdown.
- (assumption) E: Registry = GitHub Container Registry (ghcr.io), TENTATIVE. Open question: confirm it is free enough for internal use and that the self-hosted k8s cluster can pull (public package vs imagePullSecret with read:packages PAT). Fallback: another registry.
- (decision) G: Format/lint = Spotless + ktlint (detekt not in v1, easily added later). Auth seam = committed-but-inactive OAuth2 resource-server JWT config behind a profile/flag + README enable steps; nothing enforced in v1. README covers 4 params, add-a-REST-resource, run locally, enable auth seam.
- (decision) F+H: Local dev via docker compose up + one Gradle bootRun on 'local' profile. 4 params each have one documented home; v1 change path = README checklist (generator deferred). Success + counter-metrics per brief batch H accepted as-is.
- (event) Fast-path gap batch answered by user (A-H). Proceeding to draft full PRD.
- (event) Full PRD draft written: 13 features, FR-1..FR-38, 6 SMs + 3 counter-metrics, 7 open questions, cross-cutting NFRs, constraints. Companion addendum.md written with tech-selection depth for architecture step.
- (event) Update run: reconcile PRD with the architecture spine (2026-09-07). Source = ARCHITECTURE-SPINE.md + its memlog.
- (change) A1/OQ-2 FR-10,FR-12,4.4 desc,6.1,9,10: Redis is a HARD dependency (fail fast; readiness health group; cache adapter no try/catch; Redis failure -> SystemException -> 500). Overturns the degrade-to-Postgres assumption. Per AD-13.
- (change) A2/OQ-4 FR-21,FR-25,FR-27,Glossary,6.2,9: no local OpenTelemetry collector; Compose Stack = Postgres + Redis only; tracing is config-only locally (OTLP endpoint unset = no-op). Collector deferred. Per AD-17.
- (change) A3/OQ-6 FR-5,9: application layer may use org.springframework.stereotype + org.springframework.transaction.annotation only; component scanning wires use cases (no shared @Configuration). Domain stays framework-free. Boundary Test = ArchUnit. User confirmed 'leave as is'. Per AD-3, AD-2.
- (change) OQ-1 Glossary Registry, FR-30, 8, 9, 11: private ghcr.io package; CI push via built-in GITHUB_TOKEN (packages: write); cluster pull via charts-repo imagePullSecret. Per AD-20.
- (change) OQ-3 FR-18,9: springdoc-openapi-starter-webmvc-ui; OpenAPI JSON every profile, Swagger UI only under 'local'. OQ-5 FR-37,9: actuator exposes health/info/prometheus only, main port; /actuator/health** open under Auth Seam, rest need token.
- (change) OQ-7 FR-9,FR-29,6.1,9: no separate Gradle source set; @Tag('integration') in one src/test; shared IntegrationTestBase owns static @ServiceConnection Postgres+Redis. Per AD-21.
- (change) Folded settled architecture detail into FRs: FR-1/FR-3 (libs.versions.toml scope, Spotless 8.10.2/ktlint 1.5.0), FR-8/FR-9 (ddl-auto=validate), FR-13 (/api/v1/{resource}, PUT full update), FR-22 (built-in structured logging), FR-39/FR-40 (domain.shared.error.ErrorCode enum + ErrorKind; code/traceId/errors members; type=about:blank), 10 NFRs (no dynamic versions, buildpack heap calc), 11 (Auth Seam fail-safe). Fixed stale 'Assumptions Index (12)' xref -> (9).
- (change) 8 'Open Questions' -> '8. Questions Resolved in Architecture' (all 7, with AD refs). 9 Assumptions Index split into 9.1 still-open (FR-2 jsr305/warnings, FR-11 TTL, FR-15 example-removal, FR-33 dup-param, FR-36 test issuer, startup <10s) and 9.2 resolved-by-architecture. addendum.md: added Status note + per-section Resolved lines (sections 1-7).
- (event) Reconciliation applied. prd.md status still 'draft'; running Reviewer Gate (validation) next.
- (event) Reviewer Gate run inline (rubric walker + adversarial-general). Grade Good: 0 critical/high, 4 medium (FR-19 readiness omits Redis; Redis availability trade-off unstated; /v3/api-docs auth status unspecified; no pagination FR), ~10 low. Reports: validation-report.{html,md}, review-rubric.md, review-adversarial-general.md.
- (change) Validation fix (option A): M1 FR-19 readiness now names datasource AND Redis; M2 FR-10 adds 'Accepted cost' bullet (availability coupled to Redis); M3 FR-18 + FR-37 + 9.2 add rule that /v3/api-docs needs a token when Auth Seam enabled (PRD decision, fail-safe, flagged for architecture to absorb into AD-18 matcher); M4 FR-13 adds offset-paging contract (?page/?size + {items,page,size,totalElements,totalPages}, Starter-owned PageRequest/PageResult) per AD-10.
- (change) Validation lows fixed: FR-23 states 30s shutdown timeout; 10 Resource-footprint reworded (no numeric ceiling in v1, limits are charts-repo scope); SM-2 'first honest attempt' -> git-diff-checkable + defined; 10 startup assumption 'confirm during architecture'->'during the build'; FR-14 acknowledges the two FR-5 annotations; FR-33 notes .env is Plumbing not a parameter home; UJ-1 edge case notes Redis-down parity; title working-title caveat replaced with repo/name note; 12 + 0 intro reworded past-tense; 'Resolved in architecture' -> 'Settled by'/'Overturned by' (FR-5, FR-10, FR-25 = Overturned).
- (event) Memlog audit: all 23 entries captured in prd.md or addendum.md. Input reconciliation vs brief: not re-run - this update was scoped to architecture-spine reconciliation, brief already reflected in the finalized base PRD. Polish: light structure/prose pass, US spelling normalized.
- (event) PRD finalized. prd.md + addendum.md status: final, updated 2026-09-07. Next: bmad-create-epics-and-stories.
- (change) Error-model update (2026-09-09), reconciled from the finalized architecture spine (AD-11 amended, AD-12 retired): FR-39 rewritten from a two-type taxonomy (BusinessException/SystemException) + central ErrorCode/ErrorKind enum catalogue to a three-type taxonomy (BusinessException->400, NotFoundException->404, SystemException->500), each with exactly one constructor shape (message: String, details: Map<String, Any?> = emptyMap()), no per-instance code, no catalogue. FR-40 rewritten to match: fixed type-level Error Codes (BUSINESS_ERROR/NOT_FOUND/SYSTEM_ERROR/UNEXPECTED_ERROR) assigned by the handler; message+details echoed to the client for all three types including 500 (explicit user decision, overturns the prior 'never leak internal detail' rule for 500s). Glossary, §6.1 scope line, and §9.2 resolved-by-architecture entries updated to match. addendum.md's FR-39/FR-40 tech-selection notes updated in place.
- (event) PRD re-finalized after error-model update. status stays final, updated 2026-09-09. Flagged to user (not yet actioned, out of scope for this update): §4.2 FR-5, §4.5 FR-13, and §6.1's 'Four-Layer clean-architecture skeleton with a build-failing Boundary Test' language still describe the ArchUnit/ports-and-adapters paradigm the architecture spine retired on 2026-09-09 for the whole-repo pivot -- the PRD was never reconciled against that earlier, larger change, only against this narrower error-model one.
